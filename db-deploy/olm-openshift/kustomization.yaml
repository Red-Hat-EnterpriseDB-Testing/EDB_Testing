# OpenShift OLM — cluster-wide operator subscription only.
# EDB pull secret and CSV approval are manual; see README.md in this folder.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - subscription-cluster-wide.yaml

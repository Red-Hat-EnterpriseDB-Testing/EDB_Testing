# Sample only: set real values before apply (kubectl create secret / ExternalSecrets preferred).
# Username must match bootstrap.initdb.owner in cluster.yaml (default: app).
apiVersion: v1
kind: Secret
metadata:
  name: app-db-credentials
  namespace: edb-postgres
type: kubernetes.io/basic-auth
stringData:
  username: app
  password: REPLACE_WITH_A_STRONG_PASSWORD

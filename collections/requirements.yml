---
# Installed during AAP project sync from the repository root.
# (Controller looks for collections/requirements.yml here, not under playbooks/.)

collections:
  - name: amazon.aws
    version: ">=8.0.0"
    source: https://galaxy.ansible.com

  - name: community.aws
    version: ">=8.0.0"
    source: https://galaxy.ansible.com

  - name: community.postgresql
    version: ">=3.0.0"
    source: https://galaxy.ansible.com

  - name: community.general
    version: ">=9.0.0"
    source: https://galaxy.ansible.com

  - name: ansible.posix
    version: ">=1.5.0"
    source: https://galaxy.ansible.com

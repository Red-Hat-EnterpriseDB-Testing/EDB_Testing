---
# Development-oriented defaults for PostgreSQL cluster deployment

# Cluster identification
cluster_name: dev-db
namespace: development
environment: development

# Minimal sizing
instances: 1
min_available_instances: 1
enable_pod_disruption_budget: false

# Storage
storage_size: 10Gi
storage_class: local-path
storage_wal_size: 5Gi

# PostgreSQL version
postgres_version: "16.8"

# Minimal resources
resource_requests_memory: 512Mi
resource_requests_cpu: 250m
resource_limits_memory: 1Gi
resource_limits_cpu: 1000m

# PostgreSQL tuning (light)
pg_max_connections: "100"
pg_shared_buffers: "128MB"
pg_work_mem: "4MB"
pg_maintenance_work_mem: "64MB"
pg_effective_cache_size: "256MB"

# Monitoring (optional)
monitoring_enabled: false
monitoring_pod_monitor: false

# Backups (disabled for dev)
backup_enabled: false

# Wait
wait_for_cluster_ready: true
wait_timeout: 300
wait_retries: 60

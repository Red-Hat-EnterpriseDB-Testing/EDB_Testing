---
- name: Deploy development PostgreSQL cluster
  ansible.builtin.include_role:
    name: edb.postgres_operations.deploy_cluster

- name: Verify deployment
  ansible.builtin.debug:
    msg:
      - "Development cluster deployed: {{ cluster_name }} in {{ namespace }}"
      - "Instances: {{ instances }}"

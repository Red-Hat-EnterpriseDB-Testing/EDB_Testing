---
galaxy_info:
  role_name: install_postgres_rhel
  author: EDB Engineering Team
  description: Install EDB Postgres on RHEL (repository, packages, config, firewall, app user/database)
  company: EnterpriseDB
  license: Apache-2.0
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions:
        - 8
        - 9
  galaxy_tags:
    - postgresql
    - database
    - rhel
    - edb
    - postgres
    - install

dependencies: []

collections:
  - community.postgresql
  - ansible.posix

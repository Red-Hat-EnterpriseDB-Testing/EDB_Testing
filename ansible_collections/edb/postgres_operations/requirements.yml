---
# Ansible Galaxy requirements for installing collection dependencies.
# Install with: ansible-galaxy collection install -r requirements.yml
# (Optional; galaxy.yml already declares collection dependencies.)

collections:
  - name: kubernetes.core
    version: ">=2.3.0"
  - name: community.postgresql
    version: ">=2.0.0"
  - name: ansible.posix
    version: ">=1.4.0"
  - name: community.general
    version: ">=5.0.0"

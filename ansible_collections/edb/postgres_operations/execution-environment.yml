---
# Execution environment definition for Ansible Automation Platform (AAP).
# Build from this directory (collection root): ansible-builder build -t edb-postgres-operations-ee:1.0
# See README "Execution environment (AAP)" section for full steps.

version: 3

dependencies:
  galaxy: requirements-ee.yml
  python: requirements.txt

images:
  base_image:
    name: quay.io/ansible/ee-minimal-rhel9:latest

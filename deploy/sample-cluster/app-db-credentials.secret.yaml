# Replace password before apply (or use kubectl create secret / ExternalSecrets).
apiVersion: v1
kind: Secret
metadata:
  name: app-db-credentials
  namespace: edb-pg-demo
type: kubernetes.io/basic-auth
stringData:
  username: changeme
  password: changeme

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dr-test-scripts
  namespace: edb-postgres
  labels:
    app: dr-testing
    component: scripts
data:
  dr-failover-test.sh: |
    # Script content would be injected here from /scripts/dr-failover-test.sh
    # In production, build this into a container image instead
    echo "DR test script - see container image"

  validate-aap-data.sh: |
    # Script content from /scripts/validate-aap-data.sh
    echo "Data validation script - see container image"

  measure-rto-rpo.sh: |
    # Script content from /scripts/measure-rto-rpo.sh
    echo "RTO/RPO measurement script - see container image"

  generate-dr-report.sh: |
    # Script content from /scripts/generate-dr-report.sh
    echo "Report generator script - see container image"

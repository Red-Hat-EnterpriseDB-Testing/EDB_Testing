---
galaxy_info:
  role_name: deploy_replica_cluster
  namespace: edb.postgres_operations
  author: EDB Engineering Team
  description: Deploy PostgreSQL replica clusters with EDB Postgres for Kubernetes
  license: Apache-2.0
  
  min_ansible_version: "2.14"
  
  platforms:
    - name: EL
      versions:
        - "8"
        - "9"
    - name: Fedora
      versions:
        - all
    - name: Ubuntu
      versions:
        - focal
        - jammy
  
  galaxy_tags:
    - postgresql
    - database
    - kubernetes
    - openshift
    - edb
    - replication
    - replica
    - disaster_recovery
    - high_availability

dependencies: []
